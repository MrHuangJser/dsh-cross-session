# pnpm refuses to run dependency build scripts unless they are allowed here.
# Both of these are build tools whose install step only fetches or links their
# own native binary: esbuild backs vitest's transform pipeline, rolldown backs
# the `tsdown` bundler used by `pnpm run build`.
allowBuilds:
  esbuild: true
  rolldown: true
